# This is an example configuration with comments. This comment is not
# associated with any variable since it is contiguous with a variable
# (there's a blank line between this comment and the next).

# This comment is in test3.config

input_path /u/drspeech/data

# Comments are collected for ALL instances of a variable,
# so the INCLUDE will be read regardless of which variable
# you're getting the doc string for.

INCLUDE test4.config
